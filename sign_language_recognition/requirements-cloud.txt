# Streamlit Cloud 优化版本
# 使用更灵活的版本要求以确保兼容性

opencv-python-headless>=4.8.0
mediapipe>=0.10.0,<0.11.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
Pillow>=10.0.0
streamlit>=1.28.0

# 注意：
# - 使用 opencv-python-headless 而不是 opencv-python（服务器环境）
# - mediapipe 使用范围版本而不是固定版本
# - 移除了 pygame（Web应用不需要）
